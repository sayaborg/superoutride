# Next task checkpoint

## Current state

- One graph scene serves LINEAR, SEAM, CIRCUIT and BRANCH using resident RGB555 ground.
- Static compiled readers are shared; occurrences, actors, field locks and Session state remain separate.
- Performance acceptance is open: retain the regression gate and K's public CUSTOM/device HUD check.
- Vehicle physics/values, audio, input, camera, Sprite Tool and immutable mechanics/audio references remain frozen.
- The approved ground/background redesign may revise image references with causal pixel expectations and reported reasons.
- Build generates per-vehicle envelopes, continuous reference runs and compact Session budgets in disposable outputs.
- DEV_UNCALIBRATED courses and numeric observations are tool inputs, not reconstructed master content.
- The Band trial is offline tooling; the product still uses its current ground and image schemas.

## Milestones

- **P2 PR-A — Measured, release blocked:** reproduce resident/Band comparisons with `tools/performance/course-scene.mjs --ground resident|bands --frames 300 --rivals 16`; `--stills <directory>` writes comparable frames and source-footprint row metadata. The trial retains **64** as the proposed admission ceiling and **0.5** as the tested transparency threshold; 64 is not yet a qualified runtime budget. Course maximum active counts are LINEAR/SEAM/CIRCUIT **7**, BRANCH **5**. Current content alone cannot qualify 64. Slower rendering blocks PR-B; see Open decisions. Full test/CI success remains required for this offline PR.

- **P1 (3) — Next, independent of the ground decision:** replace `rival-driver` with one envelope driver accepting envelope, utilization, speed cap and target lane. Rules/Session own one provisional rival utilization; reference utilization is configuration, never a gameplay constant. Derive windows from actual lookahead. Share successive Guide samples and precompute braking/curve bounds so 16 rivals do not repeat the current 190 queries and envelope scans per tick. Report fixed-step time and allocation with 16 rivals; retain the performance gate. Establish one norm convention in its owner without changing mechanics arithmetic. Consolidate the three rival integration suites into one current-contract suite using one or two vehicles, without increasing total test time. Publish CIRCUIT with Testarossa rivals for K to judge competing speed; defer tuning to K after real content exists.

- **P2 PR-B — Blocked on the trial decision:** replace the product ground in all four modes and remove its old representation in the same PR. The following is the approved **target**, not an implemented contract:
  - Ground is an ordered list of Bands `{start, end, left, right, color: RGB555 | null, role?: pavement | shoulder | median}`; later Bands cover earlier Bands. Role-bearing Bands alone form the admitted nonoverlapping, gap-free physical partition. Visual Bands may overlap. Outermost Bands may declare one open side extending to the screen edge; authored coordinates remain finite.
  - Grass, asphalt, markings, kerbs, arrows, text and transparent cliffs are all Bands. Source authoring permits only compile-time longitudinal repetition and decomposition of metre-scale polygons into s-monotone Bands. Commit sources, never expanded Bands. Runtime retains no texture, palette, tile, periodic-pattern or polygon representation; no PNG-to-Band conversion.
  - Sweep active Bands by s and resolve their ordered lateral intervals. Filter each pixel with one exact rectangle/Band area calculation in coupled `(s,l)` coordinates using piecewise-linear boundaries. Average visible opaque area in expanded 8-bit RGB and round to RGB555; new colors are allowed. At transparent coverage at or above the qualified threshold leave BG unchanged; below it normalize only opaque area. There is no intermediate alpha. Skip fully transparent scanlines. Qualify work against active count, including distant footprints.
  - One content ceiling replaces color/dictionary/residency budgets: simultaneously active Bands. Build sweeps every Section, reports each course's maximum and distribution and rejects excess with a diagnostic. Record the qualified value from the trial above.
  - Include a compiled polygon arrow and a transparent cliff in at least one course each; preserve the cliff fixture's causal intent. Report before/after render median/p95, fixed-step median and allocation; compare Band counts/bytes with current ground payloads (LINEAR 5.85 MB, SEAM 6.67 MB, BRANCH 8.89 MB, CIRCUIT 0.96 MB). Publish all four modes for K to inspect arrows, cliffs and distant dash stability.
  - Delete ground images/references (`asphalt`, `grass`, `stripe`, `shoulder`), `presentation.ground`, `baseRgb555`, alternates and stamps. Retire `src/groundmap`, resident compiler/reader/source oracle, course-ground adapter, recipes/limits/levels, manifests/payloads, build/delivery verification and residency HUD/docs. Rewrite meaningful causal fixtures/tests and delete obsolete ones. Remove obsolete quantization, capacity-model and residency/variation prose.

- **P2 PR-C — After ground, browser release:** replace the background and sprite image representation together:
  - BG is an authored list of planes, initially one. Each plane is an 80×28 map of 16×16 tiles (1280×448 px), with pattern and palette numbers. Reuse `environments[].background`; author horizon, `pixelsPerRadian = 1280 / (2π)` and parallax distance `D`. Yaw wraps horizontally, pitch shifts rows 1:1, lateral camera motion shifts `200 * deltaX / D`. Composite back-to-front planes, then ground, then sprites; transparent foreground indices reveal earlier planes. Adding a second plane requires data only.
  - Both sprites and BG patterns use 4bpp indices: 0 transparent, 1–15 opaque. RGB555 palettes have 16 entries with unused entry 0; use direct indexing and palette offsets `paletteId << 4`, with no palette-count cap. Sprite patterns keep arbitrary dimensions and their existing area-average/nearest-palette LOD; BG tiles have no LOD.
  - Per-instance palette choice is immutable, including a selected braking palette; no mutable shared palette animation. Demonstrate brake lamps on one vehicle. Retire the 15-entry/index-minus-one convention, procedural `far-background.ts`, and old hill/coast sources; create clearly multicolored dummy tiles with different tile palettes.
  - Report pattern/palette/map bytes and render median/p95. Revise image references with reasons while preserving mechanics/audio hashes. Publish all modes for K to inspect multicolored tiles, below-horizon BG through cliffs and brake lamps.
  - Consolidate graphics/visual/terrain/render responsibilities and disambiguate the two course-presentation modules while replacing their owners; remove the single-file terrain directory as appropriate. Explain any surviving groundmap code. Preserve layer checks.
  - Move each implemented contract once to its owner: Bands/colors in content-and-gameplay, analytic drawing/filtering in architecture, pattern/palette/BG in image-assets, active-Band budget in development. Remove superseded text and distinguish remaining targets. Topic documents contain no milestone names.

- **P3 — After the representation change:** remeasure fixed-step median ≤3 ms, frame p95 ≤10 ms and GC spikes. If unmet, put measured storage/reader API changes versus target revision in Open decisions. Do not thin or simplify distant rival physics. Put seam timing outside the engine or inject a clock consistently with browser/frame-loop.

- **P4 — After P2, before N4, separate PRs:** split createCourseDrivingSource's Guide/height/terrain/presentation/sprite/seam responsibilities without behavior or performance changes; divide content-and-gameplay by authority and update the index; inventory test-only exports and close test-only interfaces, excluding physics primitives and fixtures.

- **N4 — Pending:** add a Cool Riders-style Left/Middle/Right fork using ordinary lock, traversable median, legal-route closure recovery, exit/merge and rival retarget rules. No three-way engine special case. Publish a complete start-to-results drive.

- **N5 — Pending:** use timeline observations, template-free fitting and the current vehicle envelope/unified driver; start with synthetic data if K's 30–90 second video/version is absent. Report passage-time tables and same-time images. Rules/Session own initial utilization choices 0.55/0.75/0.95. Retain observations and fit inputs only so changing vehicle values can regenerate validation. Publish the video-derived drive when inputs exist.

- **N1–N3, P1 (1)(4) — Implementation complete:** shared published scene, authored Sessions/clocks, resident ground and cached envelopes/reference budgets; performance/device acceptance remains open.

Every playable release requires complete Node 24 npm test, exact mechanics/audio comparison, exact-head CI, ahead > 0 / behind = 0 / merge base = freshly fetched main, force=false fast-forward and main=head=PR merge SHA. Verify main-push CI, Pages version/commit-versioned boot and every current manifest SHA-256; retire the included branch. Offline releases still require complete tests and exact-head CI. K's actual device driving completes playable acceptance. After N5: real content → K reviews time margins/rival speed → collisions, traffic and interactions (including cones) → inspection/fine-adjustment GUI.

## Open decisions

- **Ground performance — Before product implementation:** Node 24 / 16 rivals / 300 frames: resident → trial render median/p95 ms is LINEAR 1.57/1.97 → 2.80/3.48, SEAM 1.32/1.59 → 2.58/3.12, CIRCUIT 2.97/3.44 → 3.87/4.36, BRANCH 2.94/3.31 → 3.71/4.25. Render allocation MB/frame is 0.33/0.12/0.10/0.10 → 1.54/2.16/0.98/0.92. Exact footprint integration crosses up to 82 s-slabs despite only seven simultaneously active Bands, even with offline occlusion resolution. Recommend a measured exact-area sweep/storage redesign before proceeding; center-active count alone does not bound this work. Do not substitute point sampling.
- **Release performance — Blocking:** current main already fails its unchanged allocation gate. Preserve mechanics/audio and the gate; choose a separately reviewed storage/reader change or an explicit budget revision after the required measurements. The earlier cleanup release exception does not authorize releasing this work with failing CI.
- **Audio — K:** select the supported set of six currently selectable models or retire alternatives; recommend K decides before a separate audio change. Audio and its hashes remain frozen.
- **Progress — Separate PR:** retain circuit/ordered engines or represent loops through ordered progress; recommend proving lap, gate, recovery and clock equivalence first.
- **Pages — Separate PR:** choose dist fallback/dev publication and rebuilt-output versus uploaded-artifact verification; recommend commit-versioned product-only delivery and uploaded-artifact verification.
- **UI language — K:** choose English throughout or Japanese only in DEV; recommend English for labels, tooltips and accessibility.
