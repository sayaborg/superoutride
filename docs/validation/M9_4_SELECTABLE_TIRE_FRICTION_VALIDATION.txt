SUPER OUTRIDE — M9.4 SELECTABLE TIRE FRICTION VALIDATION
2026-09-01 JST

Scope:
Expose the current semi-slick tire reference friction plus exact 1.5x, 2.0x and 2.5x comparison
profiles for the browser player. Preserve the existing one-k tire law, compiled vehicle profiles,
relative SurfaceMap materials and common contact/wheel mechanics.

Repository:
sayaborg/superoutride

Audited main baseline:
f628e134176f735ccb4b9dc99b3db6d048e16f7d

Candidate branch:
codex/m9-4-selectable-tire-friction

Pre-validation candidate head:
e077ab682e199b428e539fd63607d2056d9362e8 feat: add selectable tire friction profiles

Pre-validation exact-head CI:
GitHub Actions run 33431740381
build job 99618470136
expected head SHA = run head SHA = PR head SHA = e077ab682e199b428e539fd63607d2056d9362e8
conclusion = success

Package / visible milestone:
super-outride-m9-4@0.9.4
SUPER OUTRIDE M9.4

Current normative authority:
docs/94_m9_4_selectable_tire_friction.md

Resolved architecture:
- one ArcadeVehicleState.tireFrictionCalibration owns one positive finite
  referenceFrictionMultiplier;
- the selected capacity is exactly muRef * referenceFrictionMultiplier * surface.gripFactor * N;
- the compiled car muRef remains 1.35 and the compiled bike muRef remains 1.25;
- tire normalized stiffness, corner stiffness, rhoKnee, low-speed regularization, radial C1
  transition, combined-slip allocation, rolling resistance and implicit wheel solve remain the
  existing ordinary authorities;
- SurfaceMap retains relative gripFactor only and gains no absolute mu or selector state;
- browser choices are exactly SEMI=1.0x, 1.5x, 2.0x and 2.5x, defaulting to SEMI;
- keyboard G cycles one common table and touch exposes every value directly;
- the common HUD reads only the current vehicle-owned value and owns no mechanics state;
- selections apply immediately, survive recovery and pass through DEV safe-spawn profile
  reconstruction without shared mutable state;
- FR, MR, RR, AWD, BIKE1 and BIKE2 use the same tire/contact/wheel path;
- ordinary construction and rivals retain the default multiplier 1.0;
- input, steering calibration, camera, renderer, route, race and topology remain unchanged.

Dedicated executable acceptance:
tests/m9-4-selectable-tire-friction.test.mjs
tests/m9-0-pedal-and-tire-causality.test.mjs
tests/m7-3-grip-and-instrument-hud.test.mjs
tests/m7-4-transient-tire-response.test.mjs
tests/m9-1-mobile-selector-controls.test.mjs
tests/m8-3-course-mode-debug.test.mjs
tests/pages-versioned-boot.test.mjs
tests/repository-hygiene.test.mjs
tests/source-boundary-normalization.test.mjs

The acceptance matrix covers:
- exact choices, labels, SEMI default, G cycling and wrap;
- exact 1.0/1.5/2.0/2.5 Fmax scaling with unchanged muRef/stiffness/rhoKnee;
- unchanged multiplicative SurfaceMap ratios;
- causal saturated lateral-acceleration and trajectory change through the common wheel path;
- invalid multiplier rejection before mutation;
- recovery and FR-to-MR reconstruction persistence with independent state objects;
- shared keyboard/touch/HUD authority and all three browser composition roots;
- ordinary 1.0 construction defaults and retained vehicle, tire, input, camera, renderer, recovery,
  route, race, topology and Pages suites.

Pre-validation local repository result:
npm test
575 tests
575 pass
0 fail
0 skipped

Browser verification:
- local LINEAR, BRANCHING and CIRCUIT loaded the M9.4 ESM tree with the matching active course,
  320x240 canvas and SEMI default;
- keyboard G advanced SEMI to 1.5x with exactly one active tire button;
- direct touch selected 2.5x, and MR safe-spawn reconstruction retained 2.5x together with the
  existing steering defaults;
- two screenshots 1.2 seconds apart had different byte hashes, proving live frame progression;
- 390x844 portrait kept the seven selector groups, 320x240-aspect canvas and driving controls
  inside a 390x844 document with no overflow;
- 844x390 landscape kept the selector groups, canvas and driving controls inside an 844x390
  document with no overflow;
- the browser console emitted no warning or error entries during the completed checks.

Validation-inclusive exact-head preconditions:
1. This artifact and its entry-document references are part of the only release candidate.
2. Push the validation-inclusive head to PR #108 targeting main.
3. Require expected SHA = actual Actions checkout SHA = PR head SHA.
4. Require the complete suite to remain 575 / 575 pass, 0 fail, 0 skipped.
5. Re-fetch main before release and require ahead_by > 0, behind_by = 0 and merge base = current main.
6. Never add an unvalidated merge commit or force-update main.
7. Move main only to the validated exact SHA with force=false.
8. Verify main SHA = validated SHA = PR head SHA = PR merge SHA and then verify the main-push Pages
   workflow and public version.txt on that same SHA.

Self-reference avoidance:
Do not edit this artifact merely to insert the later validation-inclusive candidate SHA, run id, PR
merge identity or Pages identity. Those facts are supplied by immutable Git/PR/ref/workflow history
together with this validation record.

Handling status:
DEV_UNCALIBRATED. These selections are controlled friction comparisons and do not freeze final
product tire or vehicle handling values.
