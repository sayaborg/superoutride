SESSION CONFIGURATION / COURSE STRUCTURE BOUNDARY — INITIAL VALIDATION
Recorded 2026-09-08 JST

Authority / scope
  docs/117_session_configuration_boundary.md supersedes document 61 only for
  ownership of rivalCount. SessionConfiguration owns the retained integer 0..16
  envelope. Course structure, route/finish rules, roster identity, public counts,
  physics, renderer and recovery/progress behavior remain unchanged.
  This normative boundary change requires a standalone record under this archive's
  policy. Ordinary preceding cleanup releases use their exact PR/CI evidence.

Base released main
  5283c8f0971b9a296b47bbfb4011e73675aba4a3 (PR #150)

Implementation / documentation / regression head, BEFORE this record
  d5cefc589f6ca51337d86bc6cb93fd94c733a29b
  PR #151: https://github.com/sayaborg/superoutride/pull/151
  Complete green feature CI:
    https://github.com/sayaborg/superoutride/actions/runs/34159197967
  Exact-checkout log: expected and actual both equal the full SHA above.
  Result: 853 tests, 853 passed, 0 failed.
  CI builds pinned b70f2456fe43b31d55848c4f830d4285f70147e1 on the same
  engine/host for exact hot-path state/output and renderer reference comparisons.

Causal / architecture coverage
  tests/session-configuration.test.mjs:
    Same unchanged compiled LINEAR, BRANCHING and CIRCUIT course objects pair
    with 0/1/16 rivals. Empty roster is ordinary frozen data; player is separate.
    No cardinality field remains in CourseMode; no route kind enters SessionConfiguration.
    Roster iteration consumes session data, not route shape or a copied maximum.
  tests/m6-43-course-mode-rival-roster.test.mjs and retained fixture/integration tests:
    Invalid count input rejects at its new owner. Historical/public counts are
    retained. Existing physical gate, branching arbitration, recovery, multi-actor
    and finite circuit-window assertions remain part of the complete suite.
  tests/m9-1-mobile-selector-controls.test.mjs:
    An unbound extra course can precede existing catalog entries without changing
    the explicit BRANCHING fallback. Unknown/default selection never returns an
    entry outside the supplied catalog; a missing default rejects explicitly.

Intermediate verification
  An earlier local candidate had one obsolete source-wiring assertion referring
  to selectedCircuit.courseMode. It was moved to selectedCircuit.session; physical
  behavior assertions were not weakened. Full local suite then passed 853/853.
  A final inspection found the catalog fallback depended on array position.
  d5cefc5 fixes this through the existing explicit query identity and regressions;
  the complete green CI recorded above includes that correction.

Final validation-inclusive release requirement
  This record necessarily precedes its own validating SHA. Run the complete suite
  again on the record-inclusive exact PR head. Re-fetch main, require positive
  ahead / zero behind / merge-base=current main, then fast-forward force=false.
  Verify main == PR head == PR merge SHA and green main-push/Pages on that SHA.
  Inspect the same-SHA versioned Pages artifact. Final identity is Git/PR/workflow
  evidence, not a self-referential edit to this immutable file.

Limits
  This is structural ownership and regression evidence, not a new gameplay mode,
  Traffic policy, rival roster design, start rule or handling calibration.
  Brake capacity/bias, combined braking/steering acceptance, terrain/support
  envelope and target-device browser certification remain open. Tire calibration
  and station differentiation remain paused. Core completion is not claimed.
