# Sprite preview fixtures

`fixtures/sprite-source.ts` supplies editable sprite-source examples to the Sprite Tool build.
`fixtures/sprite-lod.ts` supplies image/filter examples to the Sprite LOD preview and its build.
Both are authoring aids, separate from product course content.
