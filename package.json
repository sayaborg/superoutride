{
  "name": "super-outride",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "node tools/build/clean-dist.mjs && tsc -p tsconfig.json && node tools/build/build-sprite-preview.mjs && node tools/build/build-course-content.mjs",
    "check": "npm run lint && npm run format:check && tsc -p tsconfig.json --noEmit",
    "test": "npm run lint && npm run format:check && npm run build && node --test 'tests/**/*.test.mjs' && node tools/performance/check-course-scene.mjs",
    "lint": "eslint src tests tools",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "compile:course": "node tools/course/compile-course.mjs",
    "build:sprite-lod": "node tools/build/build-sprite-lod.mjs",
    "build:sprite-source": "node tools/build/build-sprite-source.mjs",
    "course": "node tools/course/course.mjs"
  },
  "devDependencies": {
    "eslint": "10.10.0",
    "pngjs": "7.0.0",
    "prettier": "3.9.6",
    "typescript": "^5.8.3",
    "typescript-eslint": "8.70.0"
  }
}
