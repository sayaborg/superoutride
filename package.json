{
  "name": "super-outride-m6-13",
  "version": "0.6.13",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node tools/build-ground-map.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
