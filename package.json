{
  "name": "super-outride",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "node tools/clean-dist.mjs && tsc -p tsconfig.json && node tools/build-ground-map.mjs",
    "check": "npm run lint && npm run format:check && tsc -p tsconfig.json --noEmit",
    "test": "npm run lint && npm run format:check && npm run build && node --test tests/*.test.mjs",
    "lint": "eslint src tests tools",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "10.10.0",
    "prettier": "3.9.6",
    "typescript": "^5.8.3",
    "typescript-eslint": "8.70.0"
  }
}
