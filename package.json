{
  "name": "super-outride-m7-4",
  "version": "0.7.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node tools/build-ground-map.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
