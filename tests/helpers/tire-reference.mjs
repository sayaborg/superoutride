// Pin the original causal conditions when browser tuning changes. No production default override.
export const TIRE_REFERENCE = Object.freeze({
  gripX: 2.5,
  peakSlipX: 0.08,
  gripY: 2.2,
  peakSlipY: 0.1,
  knee: 0.74,
});
